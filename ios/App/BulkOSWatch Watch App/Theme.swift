//
//  Theme.swift
//  BulkOSWatch Watch App
//
//  BulkOS "Iron & Volt" palette for the watch UI.
//

import SwiftUI

enum Theme {
    /// Neon acid-lime accent, matching the phone app.
    static let accent = Color(red: 0.80, green: 0.95, blue: 0.20) // #CCF232
    static let bg = Color.black
    static let card = Color(white: 0.10)
    static let card2 = Color(white: 0.14)
    static let fg = Color.white
    static let fg2 = Color(white: 0.70)
    static let fg3 = Color(white: 0.50)
    /// Dark ink for text on the lime accent.
    static let onAccent = Color.black
}
